<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd" >
<sqlMap namespace="nike_space">
	<typeAlias  alias="egovMap"  type="egovframework.rte.psl.dataaccess.util.EgovMap"/>
	<typeAlias  alias="nikeVO" type="egov.service.NikeVO"/>
	
	<select id="nikeDAO.selectGoodsList" resultClass="egovMap">
	
		SELECT rownum rn, a.* FROM (
								SELECT	unq,
						        title,
						        name,
						        to_char(rdate,'yyyy/mm/dd') rdate,
						        to_char(udate,'yyyy/mm/dd') udate,
						        email,
						        sns,
						        hits,
						        (select count(*) from review_comment where review_comment.p_unq = review_board.unq) commcnt					     
						FROM
						        review_board
						ORDER BY
						        unq DESC 
	
	</select>
	
	
	
	
</sqlMap>